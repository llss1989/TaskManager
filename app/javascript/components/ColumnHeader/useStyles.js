// import { makeStyles } from '@material-ui/core/styles';

const useStyles = () => ({
  root: {
    height: 42,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
});

export default useStyles;
